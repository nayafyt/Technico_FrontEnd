export enum RepairType {
  Painting = 0,
  Insulation = 1,
  Frames = 2,
  Plumbing = 3,
  ElectricalWork = 4
}

export enum StatusType {
  Pending = 0,
  InProgress = 1,
  Completed = 2,
  Declined = 3
}

export enum PropertyType {
    Detached = 0,
    Maisonet = 1,
    Apartment = 2
}

export enum UserType {
    User = 0,
    Admin = 1
}